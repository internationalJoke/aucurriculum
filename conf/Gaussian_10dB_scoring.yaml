defaults:
  - _aucurriculum_score_
  - _self_

results_dir: /mimer/NOBACKUP/groups/ulio_inverse/zhiping/data
experiment_id: Gaussian_10dB

hydra:
  sweeper:
    params:
      curriculum/scoring: CumulativeAccuracy

correlation:
  correlation_matrix: all
